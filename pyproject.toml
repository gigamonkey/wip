[project]
name = "wip-server"
version = "0.1.0"
description = "HTMX web app for managing work queues across multiple projects"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "markdown-it-py>=4.0.0",
]
